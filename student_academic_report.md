# 🎓 [초격차 탐구 보고서] 행렬의 거듭제곱과 경우의 수를 활용한 물류 경로 최적화(TSP) 시뮬레이션

## 1. 탐구 인적 사항 및 개요
* **교과 연계**: 고등학교 1학년 공통수학1 (행렬과 그 연산, 순열과 조합)
* **진로 희망**: 산업공학, 물류시스템공학, 데이터사이언스, 컴퓨터공학
* **탐구 주제**: 인접 행렬의 거듭제곱 성질과 순열·조합 알고리즘을 결합한 외판원 문제(TSP, Traveling Salesperson Problem)의 수학적 탐구 및 인터랙티브 시뮬레이터 설계

---

## 2. 탐구 동기 및 목적
현대 물류 산업 및 네비게이션 서비스에서 최적의 이동 경로를 찾는 것은 자원의 낭비를 최소화하고 효율성을 극대화하기 위한 핵심 과제이다. 고등학교 1학년 공통수학1 과정에서 학습하는 **행렬의 연산**과 **경우의 수(순열과 조합)** 개념이 단순한 연산 도구를 넘어, 어떻게 복잡한 네트워크 구조를 정형화하고 최적화 알고리즘에 활용될 수 있는지 확인하고자 본 탐구를 시작하였다.
특히, 거점의 개수가 늘어남에 따라 경로의 수가 기하급수적으로 증가하는 **'조합 폭발(Combinatorial Explosion)'** 현상을 직접 프로그래밍(JavaScript)으로 시뮬레이션하여 데이터로 확인하고, 대안적 해결책을 탐구함으로써 산업공학과 알고리즘 분석 간의 융합적 시각을 기르는 것을 목적으로 한다.

---

## 3. 교과 개념의 수학적 모델링 및 분석

### 가. 인접 행렬 및 연결 행렬 ($M$) 모델링
네트워크 지도상에 존재하는 거점들을 그래프의 **정점(Vertex, $V$)**, 정점들을 잇는 도로를 **간선(Edge, $E$)**으로 정의한다.
1. **가중치 인접 행렬 $A$**: 거점 $i$에서 거점 $j$까지의 실제 물리적 거리(km)를 성분 $a_{ij}$로 삼는 $n \times n$ 정방행렬이다.
   $$A = \begin{pmatrix} a_{11} & a_{12} & \dots & a_{1n} \\ a_{21} & a_{22} & \dots & a_{2n} \\ \vdots & \vdots & \ddots & \vdots \\ a_{n1} & a_{n2} & \dots & a_{nn} \end{pmatrix}$$
   * 이때 자기 자신으로 가는 경로는 $a_{ii} = 0$이며, 양방향 통행이 가능하므로 $a_{ij} = a_{ji}$가 성립하는 **대칭행렬($A = A^T$)**의 구조를 갖는다.
2. **연결 행렬 $M$**: 임의의 임계값(Threshold, 예: 화면 해상도 대비 기하학적 거리 비율 0.48 이내)을 설정하여, 두 거점이 직접 연결되었을 때는 $1$, 단절되었을 때는 $0$으로 나타내는 연결 상태 행렬을 정의한다.
   $$m_{ij} = \begin{cases} 1 & (\text{거점 } i \text{와 } j \text{가 직접 연결된 경우}) \\ 0 & (\text{그렇지 않은 경우}) \end{cases}$$

### 나. 연결 행렬 거듭제곱 ($M^2, M^3$)의 대수학적 정리 및 증명
* **정리**: 연결 행렬 $M$의 $k$거듭제곱 행렬 $M^k$의 $(i, j)$ 성분 $c_{ij}$는 **"거점 $i$에서 출발하여 정확히 $k$단계(경로 길이 $k$)만에 거점 $j$에 도달하는 경로의 수"**를 나타낸다.
* **증명 ($k=2$인 경우)**:
  $M^2 = M \times M$ 이므로 곱셈 정의에 의해 다음과 같다.
  $$(M^2)_{ij} = \sum_{k=1}^{n} m_{ik} m_{kj} = m_{i1}m_{1j} + m_{i2}m_{2j} + \dots + m_{in}m_{nj}$$
  여기서 각 항 $m_{ik} m_{kj}$가 $1$이 되기 위해서는 $m_{ik} = 1$이고 동시에 $m_{kj} = 1$이어야 한다. 즉, 거점 $i$에서 중간 경유지 $k$를 거쳐 거점 $j$로 가는 경로($i \to k \to j$)가 존재해야만 항의 값이 $1$이 된다.
  따라서 모든 중간 노드 $k$에 대해 이를 더한 우변의 합은 거점 $i$에서 거점 $j$로 가는 정확히 2단계 경로의 총개수와 일치한다. 이는 3단계($M^3 = M^2 \times M$) 이상에서도 수학적 귀납법에 의해 동일하게 성립한다.

### 다. 경로 탐색의 경우의 수와 순열 (Permutation)
시작 및 종료 거점인 기지(Base)를 제외한 나머지 배달 거점 $N-1$개를 모두 한 번씩만 방문하고 기지로 돌아오는 경로를 설계할 때, 가능한 모든 방문 순서의 가짓수는 순열 공식으로 기술된다.
$$\text{가능한 순열 경로 수} = {}_{N-1}P_{N-1} = (N-1)! \text{ (가지)}$$
* **곱의 법칙 연계**: 첫 번째로 방문할 노드를 선택하는 경우의 수 $(N-1)$가지부터 시작해 매 단계 노드가 하나씩 배제되므로, 각 단계의 독립적인 결정들을 연이어 결합하는 곱의 법칙에 의해 전체 가짓수는 $(N-1) \times (N-2) \times \dots \times 1 = (N-1)!$ 이 된다.
* **브루트 포스(완전 탐색) 최적화**: 시뮬레이터 알고리즘은 생성된 모든 순열 경로에 대해 실제 가중치 행렬 $A$의 성분들을 대입하여 합산 거리 $\sum a_{xy}$를 구하고, 이를 전수 비교하여 최솟값(Optimal)과 최장값(Worst)을 찾아낸다.

### 라. 럭키 모드 가중치 배치와 조합 (Combination)
럭키 모드에서 무작위로 $K$개의 도로에 가중치 혜택(보너스 초록 도로) 또는 제약(페널티 빨간 도로)을 부과하는 시스템은 조합의 개념을 활용한다.
1. **가능한 총 도로(간선)의 개수 $E$**: $N$개의 노드 중 순서 없이 2개를 선택하는 조합의 수와 같다.
   $$E = {}_N C_2 = \frac{N(N-1)}{2}$$
2. **가중치 도로 배치 조합**: $E$개의 도로 중에서 $K$개의 도로에 무작위로 특수 가중치를 부여하는 방법의 수는 다음과 같다.
   $${}_E C_K = \frac{E!}{K!(E-K)!} = \frac{\frac{N(N-1)}{2}!}{K! \left(\frac{N(N-1)}{2}-K\right)!}$$
3. **확률 공간 해석**: 매 판 무작위로 생성되는 맵은 총 ${}_E C_K$개의 가능한 도로 구조 배치 중 하나의 확률적 사건($P = \frac{1}{{}_E C_K}$)이 실현된 공간으로 해석할 수 있으며, 레벨이 올라감에 따라 조합의 분모가 극대화되는 대수적 특징을 보인다.

---

## 4. 시뮬레이터 알고리즘 구현 방식 및 성능 세이프 가드

1. **행렬 거듭제곱 연산 알고리즘 (`multiplyMatrices`)**:
   이차원 배열로 표기된 두 연결 행렬 $A, B$를 입력받아 $O(n^3)$의 시간 복잡도를 갖는 정규 행렬 곱 연산을 수행하고 결과를 테이블 DOM에 바인딩한다.
2. **경로 탐색기 (DFS 기반 백트래킹)**:
   사용자가 UI에서 선택한 출발지와 목적지, 그리고 단계 수(2단계 또는 3단계) 정보를 취합하여 DFS(깊이 우선 탐색) 구조로 해당 길이의 경로 체인을 추적하고, 검증된 경로는 인접 행렬 내에서 `path-active` 클래스를 추가해 시각적으로 하이라이트 처리한다.
3. **연산 지연 방지(안전 장치) 설계**:
   거점 수 $N$이 7을 초과할 때, $(N-1)! \ge 720$으로 연산량이 늘어나고 특히 $N \ge 8$일 때 $7! = 5040$ 등으로 폭증하여 자바스크립트 싱글 스레드 환경을 차단(Freeze)할 우려가 있다. 이를 예방하기 위해 $N \ge 7$ 조건에서 순열 테이블 렌더링을 차단하고 경고 문구를 송출하는 세이프 가드를 장착하여 시스템 안정성을 제고했다.

---

## 5. 탐구 결과 분석: 조합 폭발 ($O(N!)$)과 연산 스케일 대조
컴퓨터가 초당 $10^9$번의 대수 연산(FLOPS)을 수행할 수 있다고 가정했을 때, 거점 개수($N$)의 증가에 따른 연산 시간 추이를 역산한 결과는 다음과 같다.

| 거점 수 ($N$) | 경로 순열 수 ($N!$) | 슈퍼컴퓨터 예상 연산 시간 | 비유적 물리 스케일 |
| :---: | :---: | :---: | :---: |
| **4개** | 24 | 0.000000024초 | 눈깜짝할 사이 (전기 신호) |
| **6개** | 720 | 0.00000072초 | 눈깜짝할 사이 |
| **8개** | 40,320 | 0.00004032초 | 마이크로초 단위 연산 |
| **10개** | 3,628,800 | 0.0036초 | 일반 마우스 클릭 1회 이내 |
| **12개** | 479,001,600 | 0.479초 | 인간이 감지하는 약 0.5초 |
| **15개** | 1,307,674,368,000 | 21.79분 | 차 한 잔 마시는 시간 |
| **20개** | 약 $2.43 \times 10^{18}$ | 77.1년 | 인간의 평균 수명 |
| **25개** | 약 $1.55 \times 10^{25}$ | 4.9억 년 | 지구상의 공룡 번성기 스케일 |
| **30개** | 약 $2.65 \times 10^{32}$ | 8.4조 년 | 우주 나이(약 138억 년)의 600배 |

* **결론**: 선형적 노드 증가 대비 경우의 수는 팩토리얼 속도로 발산하며, 이는 단순 연산 하드웨어 성능 개선만으로는 해결할 수 없는 **NP-Hard** 문제의 본질을 보여준다. 이에 대한 대안으로 산업공학에서는 휴리스틱(Greedy), 메타휴리스틱(유전 알고리즘), 그리고 동적 계획법(Dynamic Programming) 등의 기법을 적용하여 '준최적해(Near-Optimal Solution)'를 빠르게 찾아내어 실무에 대입함을 배웠다.

---

## 6. 학생의 학업 역량 및 진로 발전 방향 (생활기록부 세특 기재 추천 예시)

> **[수학1 및 정보 교과 세부능력 및 특기사항 추천 내용]**
> "공통수학1 과정에서 학습한 행렬과 경우의 수 단원의 수학적 원리를 실생활 물류 네트워크에 접목하기 위해 직접 '배달 최적 경로 탐색 시뮬레이터'의 알고리즘을 설계하고 웹 애플리케이션으로 구현해 냄. 인접 행렬이 네트워크 경로의 방향성과 거리를 표현하는 유용한 도구임을 증명하고, 연결 행렬의 거듭제곱($M^2, M^3$) 성분이 갖는 '경유 경로의 수'라는 대수적 의미를 수학적으로 유도하여 동료 학생들 앞에서 논리적으로 발표함. 특히, 노드 수가 늘어남에 따라 연산량이 팩토리얼 속도로 증가하는 '조합 폭발' 현상을 수학적 수식과 그래프로 대조 분석하여 NP-Hard 문제의 실생활적 의미와 산업공학(물류최적화)에서의 탐욕적 알고리즘(Greedy Approach) 활용 필요성을 명쾌하게 설명함. 수학적 개념의 전산 모델링 및 알고리즘 구현 능력이 매우 뛰어나며, 문제를 깊이 있게 연구하고 시스템 안정성(안전 가드 설계)까지 고민하는 자기주도적 학업 태도가 매우 돋보임."
